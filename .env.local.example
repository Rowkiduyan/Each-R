# Local Supabase (Docker) example
#
# 1) Ensure local Supabase is running:
#    npx supabase start
#    npx supabase status
#
# 2) Copy values from `npx supabase status` into this file, then save it as `.env`.
#
# IMPORTANT:
# - Never commit `.env`.
# - `SUPABASE_SERVICE_ROLE_KEY` is powerful; use only for local scripts.

VITE_SUPABASE_URL="http://127.0.0.1:54321"
VITE_SUPABASE_ANON_KEY="<copy Publishable key from: npx supabase status>"

# Optional but recommended for scripts that need to bypass RLS / call introspection RPC
SUPABASE_SERVICE_ROLE_KEY="<copy Secret key from: npx supabase status>"

# Optional
VITE_SITE_URL="http://localhost:5173"
